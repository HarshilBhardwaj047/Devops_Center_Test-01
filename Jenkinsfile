pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
                echo "Checking out repository ..."
                checkout scm
            }
         }
     }
}
